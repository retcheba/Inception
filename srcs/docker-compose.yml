version: '3'

services:
  nginx:
    image: nginx
    container_name: NGINX
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    ports:
      - 443:443
    networks:
      - docker-network

networks:
  docker-network:
